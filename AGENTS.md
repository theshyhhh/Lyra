# Lyra 复刻协同规则

## 固定目录

### 当前复刻项目：只读参考

D:\UE\project\C++Project\Lyra

项目文件：

D:\UE\project\C++Project\Lyra\Lyra.uproject

### Lyra 原项目：只读参考

D:\UE\project\C++Project\LyraStarterGame

项目文件：

D:\UE\project\C++Project\LyraStarterGame\LyraStarterGame.uproject

### Unreal Engine 5.7 引擎源码：只读参考

D:\UE\UE_5.7\Engine

常用源码目录：

- D:\UE\UE_5.7\Engine\Source
- D:\UE\UE_5.7\Engine\Plugins
- D:\UE\UE_5.7\Engine\Build\Build.version

当前确认的引擎版本为 Unreal Engine 5.7.4。

## 1. 项目定位

这是一个以学习、理解和复刻 **Lyra Starter Game** 为主要目标的 Unreal Engine C++ 项目。

本项目并不以机械复制 Lyra 文件为最终目标，而是通过复刻过程理解：

- Lyra 各框架的职责划分。
- Gameplay Framework 对象之间的协作关系。
- Experience、GameFeature、Modular Gameplay、GAS、Enhanced Input、UI 等系统的组合方式。
- Lyra 对 Unreal Engine 原生框架的扩展方式。
- 引擎类、生命周期、资产加载、网络复制和模块系统的底层机制。
- 当前项目与 Lyra 原始实现之间的差异、缺失和有意调整。

AI 的首要职责是帮助维护一套适合长期复习、回顾和深入理解的项目文档。

------

## 2. 核心工作目标

处理文档任务时，按照以下优先级工作：

1. **解释当前项目已经复刻的内容。**
2. **梳理当前实现内部的调用关系和设计意图。**
3. **结合 Unreal Engine 源码深入解释关键类和方法。**
4. **与对应版本的 Lyra 源码进行对比。**
5. **识别当前尚未复刻、只完成部分实现或存在差异的内容。**
6. **维护文档之间的链接、目录和术语一致性。**

文档的主体必须是当前项目，而不是 Lyra 源码摘要。

Lyra 源码的主要作用是回答：

- 当前系统在 Lyra 中完整形态是什么？
- 当前已经复刻到什么程度？
- 当前缺失了哪些类、属性、方法、配置或资产？
- 当前实现与 Lyra 有哪些行为差异？
- 这些差异是暂时缺失、主动简化，还是项目自己的设计选择？

引擎源码的主要作用是回答：

- 父类究竟提供了什么能力？
- 某个重写函数由谁调用、何时调用？
- 某个接口、宏、委托、Subsystem 或组件管理器是如何工作的？
- 某段 Lyra 代码为什么必须这样写？
- 生命周期、网络、异步加载、GC、资产管理等机制在底层如何运行？

------

## 3. 信息来源与可信度顺序

分析时按照以下顺序确定事实：

### 3.1 当前项目源码

当前项目源码是判断“已经实现了什么”的唯一主要依据，包括：

- `Source/`
- 项目插件下的 `Source/`
- `.Build.cs`
- `.Target.cs`
- `.uproject`
- `.uplugin`
- `Config/`
- 可以可靠检查的资产配置、蓝图父类和 Primary Asset 信息
- 已有项目文档
- 编译日志、运行日志和测试结果

不能因为 Lyra 中存在某段代码，就默认当前项目已经实现。

不能因为当前项目中存在类名或空函数，就将其标记为完整复刻。

### 3.2 当前项目对应版本的 Lyra 源码

Lyra 源码用于比较完整实现、设计意图和系统边界。

对比前必须尽量确定：

- 当前项目使用的 Unreal Engine 版本。
- Lyra 参考项目的 Unreal Engine 版本。
- Lyra 参考源码所在目录或仓库。
- 是否存在跨版本 API 变化。

不得无提示地将不同引擎版本的 Lyra 实现混合在一起。

### 3.3 当前版本的 Unreal Engine 源码

引擎源码用于解释父类行为和底层机制。

优先阅读：

- 类声明所在头文件。
- 关键函数的 `.cpp` 实现。
- 直接调用者。
- 直接被调用的方法。
- 相关接口、Subsystem、Manager、宏和数据结构。
- 版本相关的预处理分支。

### 3.4 官方文档

官方文档主要用于补充：

- 系统设计背景。
- 推荐用法。
- API 的公开语义。
- 编辑器配置步骤。
- 跨系统概念介绍。

当官方文档与当前版本源码存在差异时，以当前版本源码为准，并在文档中指出版本差异。

### 3.5 推断和外部资料

只有在当前项目、Lyra 源码、引擎源码和官方文档仍不足以确认时，才允许进行推断。

所有推断必须明确标记为：

> ⚠️ **推断：** 以下结论根据调用关系和现有代码推导，尚未经过运行验证。

不得把推断写成已经验证的事实。

------

## 4. 开始任务前的检查

每次整理或更新框架文档前，先完成以下检查。

### 4.1 确定版本

检查并记录：

- 当前项目 Unreal Engine 版本。
- Lyra 参考版本。
- 相关插件版本或 API 差异。
- 文档上次更新时间。

如果无法确定精确版本，在文档中明确写出版本未知，不得自行假定。

### 4.2 阅读现有文档

开始修改前：

1. 阅读目标文档。
2. 阅读文档目录或索引。
3. 阅读目标文档引用的关联文档。
4. 搜索是否已经存在相同主题，避免重复创建。
5. 保留已有文档中仍然正确的内容、链接和人工备注。

不得为了统一措辞而无意义地重写整篇文档。

### 4.3 确定当前实现范围

至少检查：

- 目标类的 `.h` 和 `.cpp`。
- 直接父类和实现接口。
- 创建该对象的位置。
- 调用核心入口的位置。
- 关键属性的写入者和读取者。
- 配置文件中的注册项。
- 模块依赖和插件依赖。
- 蓝图子类或数据资产是否会覆盖 C++ 默认值。
- 网络复制注册。
- 生命周期中的初始化与清理路径。

------

## 5. 实现状态分类

比较当前项目与 Lyra 时，只能使用以下状态。

| 状态           | 含义                                                         |
| -------------- | ------------------------------------------------------------ |
| **已复刻**     | 当前项目已经实现核心结构和主要行为，与参考实现基本一致       |
| **部分复刻**   | 已实现主要结构，但缺少重要分支、配置、清理、错误处理或关联功能 |
| **结构占位**   | 类、属性或函数已经存在，但只有空实现、简单调用 `Super` 或 TODO |
| **未复刻**     | 当前项目中没有对应实现                                       |
| **有意简化**   | 当前项目明确选择了更简单的实现，并且该差异符合当前项目目标   |
| **项目自定义** | 当前项目行为不同于 Lyra，属于主动设计而非遗漏                |
| **待确认**     | 仅凭静态代码无法确认，需要资产检查、运行验证或网络测试       |

判断状态时必须提供代码证据。

以下情况不能标记为“已复刻”：

- 只有同名类。
- 只有函数声明。
- 函数只调用了 `Super`。
- 代码被注释。
- 只保留了 TODO。
- Lyra 中的核心配置或资产没有创建。
- 只复制了数据结构，但没有接入调用流程。
- 只实现服务器流程，客户端流程尚未验证。
- 加载流程存在，但卸载和清理流程缺失。

------

## 6. 推荐分析流程

### 第一步：从当前项目建立事实

先解释当前项目实际存在的内容：

- 系统入口在哪里？
- 对象由谁创建？
- 何时初始化？
- 谁调用核心方法？
- 数据从哪里产生？
- 数据经过哪些对象？
- 最终影响什么行为？
- 结束时如何清理？

### 第二步：建立调用链

至少追踪一条完整主流程：

```text
外部入口
  → 框架类
  → 当前项目扩展类
  → 核心组件或 Subsystem
  → 数据资产、配置或插件
  → 最终游戏行为
```

调用链必须以当前项目代码为主。

只有当前项目中不存在的后续步骤，才使用 Lyra 流程进行补充，并明确标记。

### 第三步：对比 Lyra

按类、属性、方法、配置和资产逐项对比：

- 当前项目有什么？
- Lyra 有什么？
- 差异是什么？
- 差异是否影响主流程？
- 是否需要继续复刻？
- 如果继续复刻，依赖哪些前置系统？

### 第四步：深入引擎源码

只深入解释对理解当前实现有帮助的引擎机制。

避免把文档变成与当前系统无关的引擎 API 百科。

### 第五步：更新文档

文档更新完成后：

- 检查内部链接。
- 检查类名、函数名和属性名。
- 检查 Runtime、Editor、Server、Client 标记。
- 检查实现状态。
- 检查是否把 Lyra 内容误写成当前项目内容。
- 检查 TODO 是否有代码依据。
- 检查关联文档是否需要同步更新。

------

## 7. 标准文档结构

除非目标系统非常简单，框架文档优先采用以下结构。

```markdown
# 编号 - 框架名称

> 用一段话说明这个框架解决什么问题，以及它在当前项目中的位置。

---

## 框架概述

## 当前复刻状态

## 类列表

## 核心数据流

## 初始化与运行流程

## 逐类详解

## 网络与权限

## 资产、配置与模块依赖

## 与 Lyra 的差异

## 已识别的 TODO

## 复习要点

## 关联框架
```

可以根据系统特点增删章节，但必须保证“当前复刻状态”和“与 Lyra 的差异”可以被快速找到。

------

## 8. 框架概述写法

“框架概述”应该回答：

- 当前系统解决什么问题？
- 为什么需要这个系统？
- 它位于整体架构的哪一层？
- 它与哪些其他系统交互？
- 当前项目已经实现了哪些核心能力？
- 当前实现是否是完整流程？

优先解释设计意图，不要只把类名翻译成中文。

推荐格式：

```markdown
## 框架概述

**问题：** 传统方案或当前需求中存在什么问题。

**当前项目的解决方案：** 当前项目通过哪些对象和流程解决。

**设计意图：**
- 设计目标一
- 设计目标二
- 设计目标三

**当前完成度：** 简要说明已复刻、部分复刻和未复刻内容。
```

------

## 9. 当前复刻状态写法

每篇框架文档应尽量包含一张状态表。

```markdown
## 当前复刻状态

| 模块 | 当前状态 | 当前实现 | Lyra 对应内容 | 影响 |
|---|---|---|---|---|
| Experience 选择 | 已复刻 | 已支持 URL 和 WorldSettings | Lyra 还支持命令行和专用服务器流程 | 不影响基础 PIE |
| 插件卸载 | 未复刻 | 当前没有卸载路径 | Lyra 在 EndPlay 中执行停用 | 可能导致切换或 PIE 清理问题 |
```

状态描述必须具体，避免只写“未完成”“以后处理”。

------

## 10. 类列表写法

类列表至少包含：

| 字段       | 说明                                 |
| ---------- | ------------------------------------ |
| 类         | 完整类名                             |
| 父类或接口 | 关键继承关系                         |
| 生命周期   | Runtime、Editor-Only、Server-Only 等 |
| 网络位置   | Server、Client、Both、Replicated     |
| 当前状态   | 已复刻、部分复刻等                   |
| 职责       | 一句话描述当前项目中的实际职责       |

不要直接复制 Lyra 的类列表后将其称为当前项目类列表。

------

## 11. 核心数据流写法

优先使用简单的 ASCII 流程图：

```text
入口对象
  ├── 配置来源 A
  ├── 配置来源 B
  └── 默认值
        |
        v
当前项目管理组件
        |
        v
资源加载 / 状态切换 / 组件注入
        |
        v
最终游戏对象
```

流程图之后必须用文字解释：

- 每一步由哪个函数执行。
- 哪些步骤是同步的。
- 哪些步骤是异步的。
- 哪些步骤只在服务器执行。
- 哪些结果会复制到客户端。
- 失败时当前代码如何处理。

不得绘制当前项目中不存在的流程而不加标记。

使用 Lyra 流程补充时标记：

```text
[当前已实现] → [当前已实现] → [Lyra 中存在，当前未复刻]
```

------

## 12. 逐类详解模板

每个重要类优先使用以下模板。

```markdown
### UExampleClass [Runtime，复制]

**当前状态：** 已复刻 / 部分复刻 / 其他状态

**源码位置：**
- 当前项目：`Source/.../ExampleClass.h`
- 当前项目：`Source/.../ExampleClass.cpp`
- Lyra 参考：`Source/.../LyraExampleClass.cpp`
- 引擎父类：`Engine/Source/...`

**继承链：**

`UObject → UActorComponent → UExampleClass`

**实现接口：**

- `IExampleInterface`

**UCLASS：**

`UCLASS(...)`

**职责：**

说明该类在当前项目中的实际职责。

**创建位置与所有权：**

说明由谁创建、Outer 或 Owner 是谁、生命周期由谁控制。

**初始化时机：**

说明构造、初始化、BeginPlay、Experience Loaded 等时机。

**关键属性：**

| 属性 | 类型 | 复制 | 当前用途 |
|---|---|---|---|

**关键方法：**

#### `FunctionName()`

- 调用者：
- 调用时机：
- 执行端：
- 前置条件：
- 当前实现：
- 副作用：
- 失败处理：
- Lyra 差异：
- 引擎机制：

**清理流程：**

说明 EndPlay、Deinitialize、委托解绑、异步句柄释放等。

**当前限制：**

- 限制一
- 限制二
```

简单类可以缩短，但核心类不得只写属性和函数翻译。

------

## 13. 引擎源码解释规范

解释引擎类或方法时，至少考虑以下问题。

### 13.1 调用来源

- 这个方法由谁直接调用？
- 是引擎自动调用还是项目主动调用？
- 调用发生在哪个生命周期阶段？
- 是否可能被重复调用？

### 13.2 执行环境

- 在 Game Thread 还是其他线程？
- Server、Client 还是两端都会执行？
- 是否依赖有效的 `UWorld`？
- 是否依赖特定 NetMode？
- 是否只在 Editor 或 PIE 中执行？

### 13.3 对象生命周期

- 对象由谁创建？
- Owner、Outer 和 World 分别是什么？
- 对象何时销毁？
- 是否受 GC 管理？
- 是否需要 `UPROPERTY`、弱引用或手动解绑？

### 13.4 副作用

- 是否改变复制状态？
- 是否注册委托？
- 是否添加组件？
- 是否持有异步加载句柄？
- 是否修改 Asset Manager Bundle 状态？
- 是否影响下一帧或下一次网络更新？

### 13.5 失败行为

- 使用 `check`、`ensure`、返回值还是日志？
- Shipping 构建中的行为是否不同？
- 异步失败是否有回调？
- 当前项目是否处理了失败分支？

### 13.6 源码引用格式

引用源码时使用稳定的文件路径和符号名：

```markdown
- `Engine/Source/Runtime/Engine/Private/GameModeBase.cpp`
  - `AGameModeBase::InitGame()`
- `Source/LyraGame/Experience/LyraExperienceManagerComponent.cpp`
  - `ULyraExperienceManagerComponent::StartExperienceLoad()`
```

行号可以作为临时辅助，但不要只依赖行号，因为版本更新后行号容易变化。

------

## 14. Unreal Engine 专项检查项

分析任何 UE 系统时，根据相关性检查以下内容。

### 14.1 生命周期

- 构造函数
- `PostInitProperties`
- `PostInitializeComponents`
- `OnRegister`
- `InitializeComponent`
- `BeginPlay`
- `PossessedBy`
- `OnRep_PlayerState`
- `EndPlay`
- `Deinitialize`
- Editor 生命周期
- PIE 生命周期
- Seamless Travel

只记录与当前系统有关的生命周期。

### 14.2 网络

- Authority 判断。
- 对象在哪一端存在。
- 属性是否复制。
- `GetLifetimeReplicatedProps` 注册。
- RepNotify。
- RPC 类型和可靠性。
- OwnerOnly、SkipOwner、ReplayOnly 等复制条件。
- Listen Server 与 Dedicated Server 差异。
- 客户端对象是否可能晚于 PlayerState 或 GameState 创建。
- 当前行为是否经过多人测试。

### 14.3 UObject 与 GC

- `Outer`。
- `UPROPERTY`。
- `TObjectPtr`。
- `TWeakObjectPtr`。
- 动态创建对象的持有者。
- 委托是否形成悬空引用。
- 异步对象如何防止提前 GC。
- `RegisterWithGameInstance` 或其他生命周期托管机制。

### 14.4 资产管理

- Primary Asset Type。
- Primary Asset ID。
- 扫描配置。
- Asset Bundle。
- `TSoftObjectPtr` / `TSoftClassPtr`。
- 同步加载与异步加载。
- `FStreamableHandle` 的持有和释放。
- Cook 和 Chunk 是否需要额外配置。
- 资产是通过硬引用、软引用还是 Asset Manager 发现。

### 14.5 GameFeature 与 Modular Gameplay

- 插件名称到 URL 的转换。
- Register、Load、Activate、Deactivate、Unregister 阶段。
- `UGameFeatureAction` 的执行顺序。
- World Context 限定。
- `UGameFrameworkComponentManager` 的组件注入和初始化状态。
- 多 PIE 会话的插件引用问题。
- 停用时的异步 pauser 和清理流程。

### 14.6 C++ 与 Blueprint 边界

- C++ 构造函数默认值。
- 蓝图子类是否覆盖默认类。
- 数据资产是否补充运行时配置。
- Blueprint Implementable Event 或 Native Event。
- 蓝图资产内容无法可靠检查时，标记为“待资产验证”。
- 不得仅根据 C++ 默认值断言运行时最终配置。

### 14.7 编译与构建条件

- `WITH_EDITOR`
- `WITH_EDITORONLY_DATA`
- `UE_SERVER`
- `WITH_SERVER_CODE`
- `MinimalAPI`
- 模块导出宏
- 插件依赖
- Public 和 Private 模块依赖
- Cooked 构建与 Editor 构建差异

------

## 15. Lyra 对比规范

与 Lyra 对比时，不要只列出缺少的函数名。

每个重要差异应解释：

```markdown
### 差异：异步反激活尚未接入

**当前项目：**

当前 `EndPlay()` 只执行同步清理，没有创建
`FGameFeatureDeactivatingContext`。

**Lyra：**

Lyra 使用 Deactivating Context 收集异步 pauser，
所有 pauser 完成后才进入最终清理。

**影响：**

当前简单 Action 可以正常退出，但需要异步清理资源的 Action
可能在 World 销毁时留下未完成任务。

**建议：**

在开始复刻异步停用前，先补充 Action 生命周期测试，
并验证多 PIE 与地图切换场景。

**状态：** 部分复刻
```

### 15.1 不应复刻的内容

Lyra 中存在并不代表必须复刻。

以下内容可以标记为暂不需要：

- 当前项目不会使用的平台功能。
- 在线服务特有逻辑。
- 当前阶段不使用的前端系统。
- 与项目玩法无关的 ShooterCore 功能。
- 纯调试或自动化测试代码。
- 已被当前引擎版本替代的旧 API。

但必须说明不复刻的理由以及可能影响。

------

## 16. TODO 管理规范

TODO 必须分为以下来源：

### 16.1 当前源码 TODO

当前项目源码中明确存在的 TODO。

### 16.2 Lyra 对比 TODO

Lyra 已实现，但当前项目缺失的内容。

### 16.3 验证 TODO

代码已经存在，但尚未经过运行、网络、打包或 PIE 验证的内容。

### 16.4 文档 TODO

源码已经存在，但文档解释仍不完整的内容。

推荐格式：

```markdown
## 已识别的 TODO

| 优先级 | 类型 | 内容 | 依据 | 前置依赖 |
|---|---|---|---|---|
| 高 | Lyra 对比 | 补充插件停用流程 | Lyra ExperienceManagerComponent | Action 生命周期 |
| 中 | 验证 | 验证客户端晚复制 PlayerController | 当前注释与网络流程 | 双客户端测试 |
| 低 | 文档 | 补充 Asset Bundle 底层机制 | Engine AssetManager | 无 |
```

不要把所有 Lyra 差异自动转化为高优先级 TODO。

------

## 17. 复习型文档要求

文档不仅用于记录，还必须适合日后复习。

每篇核心文档末尾尽量加入：

```markdown
## 复习要点

1. 该框架最主要解决什么问题？
2. 当前系统的入口函数是什么？
3. 核心状态保存在哪个对象上？
4. 哪些逻辑只在服务器运行？
5. 客户端如何得知系统已经准备完成？
6. 当前项目与 Lyra 最大的差异是什么？
7. 该系统依赖了哪些引擎底层机制？
```

复习要点应针对该系统编写，不能每篇完全相同。

对于复杂流程，可以增加：

```markdown
## 快速回顾

- 一句话职责：
- 核心入口：
- 核心状态：
- 关键依赖：
- 网络位置：
- 当前完成度：
- 最重要的未完成项：
```

------

## 18. 写作风格

### 18.1 语言

- 正文使用中文。
- 类名、方法名、属性名、宏、配置项和专业术语保留英文。
- 第一次出现重要术语时可以补充中文解释。
- 不要强行翻译已经约定俗成的 UE 术语。

### 18.2 内容重点

优先解释：

- 为什么这样设计。
- 对象之间如何协作。
- 代码何时运行。
- 数据如何流动。
- 网络两端有什么差异。
- 当前项目已经完成到哪里。
- Lyra 为什么比当前实现多出某些步骤。

不要只做以下工作：

- 翻译源码注释。
- 罗列所有成员。
- 重复 API Reference。
- 大段复制 Lyra 源码。
- 用宽泛概念替代具体调用链。

### 18.3 表格

适合使用表格的内容：

- 类列表。
- 属性列表。
- 实现状态。
- Lyra 差异。
- 网络执行位置。
- 配置项。
- TODO。

复杂执行流程优先使用流程图和分步骤说明，不要强行塞进大表格。

### 18.4 提示块

统一使用以下提示：

```markdown
> ⚠️ **注意：** 容易误解或存在限制的内容。

> ⏱️ **引擎调用时机：** 生命周期方法的实际调用阶段。

> 🌐 **网络：** Server、Client、Listen Server 或复制相关说明。

> 🧠 **设计意图：** 解释为什么采用当前架构。

> 🔍 **Lyra 对比：** 当前项目与 Lyra 的差异。

> 🧩 **引擎机制：** 来自 Unreal Engine 源码的底层解释。

> 🧪 **待验证：** 静态源码无法完全确认的行为。
```

------

## 19. 代码示例规范

- 优先引用当前项目代码。
- 示例只保留理解当前机制所需的部分。
- 不要复制大段 Lyra 或引擎源码。
- 省略代码时使用明确注释。
- 不得编造当前项目中不存在的方法。
- 修改后的示例必须与当前 UE 版本兼容。
- 示例如果只是概念代码，明确标记为“伪代码”。
- 代码注释重点解释意图，不重复代码字面含义。

------

## 20. 文件与链接管理

### 20.1 文件命名

延续已有编号和分类方式，例如：

```text
01-Project-Overview.md
02-Engine-Configuration.md
03-System-Framework.md
04-Game-Framework.md
07-Experience-Framework.md
```

创建新文件前先检查现有编号和主题，避免冲突。

### 20.2 内部链接

使用相对路径：

```markdown
[07-Experience-Framework.md](07-Experience-Framework.md)
```

链接到章节时使用稳定标题：

```markdown
[Experience 加载流程](07-Experience-Framework.md#加载流程详细信息)
```

修改标题后必须检查其他文档中的锚点链接。

### 20.3 关联框架

文档末尾的“关联框架”应解释关联原因：

```markdown
- [05-Player-Framework.md](05-Player-Framework.md)
  — PlayerState 在 Experience 加载完成后设置 PawnData。
```

不要只列文件名而不解释关系。

------

## 21. 文档修改边界

当用户要求整理文档时：

- 默认只修改文档。
- 不修改运行时代码。
- 不修改资产。
- 不调整模块依赖。
- 不执行大规模重命名。
- 不删除用户已有的研究结论。
- 不覆盖明确标记为人工备注的内容。

如果发现源码问题，在文档中记录，不要顺手修改代码，除非用户明确要求。

不要修改以下生成目录：

- `Binaries/`
- `DerivedDataCache/`
- `Intermediate/`
- `Saved/`
- IDE 生成目录
- 自动生成的项目文件

------

## 22. 不确定性处理

无法确认时，不要阻塞整个文档任务。

采用以下方式继续：

```markdown
> 🧪 **待验证：**
> 当前 C++ 源码已经注册该委托，但尚未检查对应蓝图资产是否绑定，
> 因此只能确认代码侧具备支持，不能确认运行时一定执行。
```

需要运行测试才能确认的内容，给出具体测试建议：

```markdown
**建议验证方式：**

1. 启动 Listen Server 和一个远程客户端。
2. 在 PlayerController 复制前记录 PlayerState 初始化日志。
3. 检查 `TryActivateAbilitiesOnSpawn()` 是否被再次调用。
4. 分别记录 Server 和 Client 的执行结果。
```

不得使用“应该没问题”“大概如此”等模糊结论。

------

## 23. 完成标准

文档任务完成前必须检查：

-  主要内容以当前项目为中心。
-  已确认当前项目的实际源码范围。
-  没有把 Lyra 内容误写成当前已实现内容。
-  每个核心类都有明确职责。
-  至少梳理了一条完整主调用链。
-  关键生命周期函数说明了调用时机。
-  网络相关类说明了执行端和复制方式。
-  关键引擎机制有针对性地深入解释。
-  当前状态使用统一分类。
-  Lyra 差异说明了影响，而不只是列名称。
-  TODO 有来源和优先级。
-  不确定内容已经明确标记。
-  文件名、类名、函数名和配置名与源码一致。
-  文档内部链接有效。
-  关联文档已同步检查。
-  没有大段复制第三方源码。
-  没有修改与任务无关的代码或文件。

------

## 24. 默认输出原则

当用户要求“整理某个已复刻系统的文档”时，默认按照以下顺序输出结果：

1. 当前项目实现概述。
2. 当前复刻状态。
3. 当前项目核心数据流。
4. 当前项目逐类解释。
5. 相关引擎源码机制。
6. Lyra 完整实现对比。
7. 未完成内容和影响。
8. 复习要点。
9. 本次修改的文件列表。
10. 仍需运行验证的内容。

最终汇报必须明确区分：

- 已根据源码确认。
- 根据 Lyra 对比发现。
- 根据引擎源码解释。
- 尚未验证。
